import '../styling/fonts.css';
import placeholder from '../assets/import_placeholder.png';

const Loading = () => {
  return <img className='gallery-item' src={placeholder} alt='' />;
};

export default Loading;
